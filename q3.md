```mermaid
classDiagram
    class Drone{
        <<interface>>
        +loadMaterial(config: const SprayConfiguration&)* void
        +flyTo(x: double, y: double)* void
        +spray()* void
    }
    class ModernDrone{
        +loadMaterial(config: const SprayConfiguration&) void
        +flyTo(x: double, y: double) void
        +spray() void
    }

    class LegacyDrone{
        +setChemicalData(chemicalRate: double) void
        +mobeToLocation(location: const string&) void
        +startChemicalRelease() void
    }

    class LegacyDroneAdapter{
        +loadMaterial(config: const SprayConfiguration&) void
        +flyTo(x: double, y: double) void
        +spray() void
    }

    Drone <|.. ModernDrone
    Drone <|.. LegacyDroneAdapter
    Drone  ..> SprayConfiguration

    LegacyDroneAdapter *-- LegacyDrone

    class FarmComponent{
        +getName()* string
        +getArea()* double
        +display()* void
        +accept()* void
        +createIterator()* std::unique_ptr~FieldIterator~
    }
    
    class FarmArea{
        -name: string
        -children: vector~unique_ptr~FarmComponent~~
        +add(component: unique_ptr~FarmComponent~) void
        +remove(name: string) void
        +getChild(index: int) FarmComponent*
        +getName() string
        +getArea() double
        +display() void
        +accept() void
        +createIterator() std::unique_ptr~FieldIterator~
    }
    
    class CropField{
        -name: string
        -cropName: string
        -area: double
        -x: double
        -y: doouble
        -growthStage: string
        -sprayed: bool
        +getName() string
        +getArea() double
        +display() void
        +accept() void
        +createIterator() std::unique_ptr~FieldIterator~
        +getX() void
        +getY() void
        +getCropName() string
        +getGrowthStage() string
        +isSprayed() bool
        +setSprayed(value: bool) void
    }

    FarmComponent <|-- FarmArea
    FarmComponent <|-- CropField
    
    FarmArea "1" *-- "0..*" FarmComponent

    class FieldIterator{
        <<interface>>
        +first()* void
        +next()* void
        +hasNext()* bool
        +current()* CropField*
    }
    
    class FarmIterator{
        -fields: vector~CropField*~
        -index: int
        +FarmIterator(root: FarmComponent*)
        +first() void
        +next() void
        +hasNext() bool
        +current() CropField*
    }

    FieldIterator <|-- FarmIterator

    FarmIterator--> "0..*" CropField
    FarmIterator ..> FarmComponent

    class SprayConfiguration{
        <<interface>>
        +getSprayVolume()* double
        +getSprayIntervalDays()* int
        +getFlightHeight()* double
        +getConfiguration()* string
        +clone()* std::unique_ptr~SprayConfiguration~  
    }
