 ```mermaid
classDiagram
    class Drink{
        <<interface>>
        +getDescrptionn()* stirng
        +getPrice()* double
        +getCalories()* int
        +getSize()* DrinkSize
        +getScugarLevel()* SugarLevel
        +getIceLevel()* IceLevel
    }

    class BaseDrink{
        -type: DrinkType
        -size: DrinkSize
        -sugar: SugarLevel
        -ice: IceLevel
        -price: double
        -calories: int
        +BaseDrink(type: DrinkType, size: DrinkSize, sugar: SugarLevel, ice: IceLevel)
        +getDescrptionn() stirng
        +getPrice() double
        +getCalories() int
        +getSize() DrinkSize
        +getScugarLevel() SugarLevel
        +getIceLevel() IceLevel
    }

    Drink <|.. BaseDrink

    class DrinkBuilder{
        <<interface>>
        +setType(type: DrinkType)* DrinkBuilder&
        +setSize(size: DrinkSize)* DrinkBuilder&
        +setSugar(sugar: SugarLevel)* DrinkBuilder&
        +setIce(ice: IceLevel)* DrinkBuilder&
        +build()* unique_ptr~Drink~
    }

    class ConcreteDrinkBuilder{
        -type: DrinkType
        -size: DrinkSize
        -sugar: SugarLevel
        -ice: IceLevel
        +setType(type: DrinkType) DrinkBuilder&
        +setSize(size: DrinkSize) DrinkBuilder&
        +setSugar(sugar: SugarLevel) DrinkBuilder&
        +setIce(ice: IceLevel) DrinkBuilder&
        +build() unique_ptr~Drink~
    }

    class DrinkDirector{
        +makeStandardBlackTea(builder: DrinkBuilder&) unique_ptr~Drink~
        +makeStandardGreanTea(builder: DrinkBuilder&) unique_ptr~Drink~
        +makeStandardFruitTea(builder: DrinkBuilder&) unique_ptr~Drink~
    }

    DrinkBuilder <|.. ConcreteDrinkBuilder

    ConcreteDrinkBuilder ..> BaseDrink
    DrinkDirector ..> DrinkBuilder

    class DrinKPreparation{
        <<abstract>>
        +prepare() void
        #prepareCup() void
        #prepareBase()* void
        #addMainIngredient()* void
        #specialProcessing() void
        #adjustSugarAndIce() void
        #finish() void
    }
    
    class BlackTeaPrepartion{
        #prepareBase() void
        #addMainIngredient() void
    }

    class GreenTeaPrepartion{
        #prepareBase() void
        #addMainIngredient() void
    }

    class FruitTeaPrepartion{
        #prepareBase() void
        #addMainIngredient() void
        #specialProcessing() void
    }

    class MilkTeaPrepartion{
        #prepareBase() void
        #addMainIngredient() void
    }

    DrinlPreparation <|-- BlackTeaPrepartion
    DrinlPreparation <|-- GreenTeaPrepartion
    DrinlPreparation <|-- FruitTeaPrepartion
    DrinlPreparation <|-- MilkTeaPrepartion

    class DrinkDecorator{
        <<abstract>>
        #_drink : unique_ptr~Drink~
        +DrinkDecorator(drink: unique_ptr~Drink~)
        +getSize() DrinkSize
        +getScugarLevel() SugarLevel
        +getIceLevel() IceLevel
    }

    class FreshMilkDecorator{
        +getDescrptionn() stirng
        +getPrice() double
        +getCalories() int
    }

    class GoatMilkDecorator{
        +getDescrptionn() stirng
        +getPrice() double
        +getCalories() int
    }

    class HoneyDecorator{
        +getDescrptionn() stirng
        +getPrice() double
        +getCalories() int
    }

    class TeaJellyDecorator{
        +getDescrptionn() stirng
        +getPrice() double
        +getCalories() int
    }

    class FreshFruitDecorator{
        +getDescrptionn() stirng
        +getPrice() double
        +getCalories() int
    }

    class CheseFoamDecorator{
        +getDescrptionn() stirng
        +getPrice() double
        +getCalories() int
    }

    Drink <|-- DrinkDecorator

    DrinkDecorator <|-- FreshMilkDecorator
    DrinkDecorator <|-- GoatMilkDecorator
    DrinkDecorator <|-- HoneyDecorator
    DrinkDecorator <|-- TeaJellyDecorator
    DrinkDecorator <|-- FreshFruitDecorator
    DrinkDecorator <|-- CheseFoamDecorator

    DrinkDecorator "1" *-- "1" Drink